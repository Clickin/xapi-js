{
  "name": "xapi-js",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "tsup": "^8.0.2",
    "typescript": "^5.0.0",
    "vitest": "^3.2.4"
  }
}